  
version: '3'
services:
  node:
    restart: always
    build: 
      context: .
      dockerfile: "./server/classifier/Dockerfile"
    volumes:
      - ./server/classifier/src:/app/src
    ports:
      - "8080:8080"
  browser:
    tty: true
    build:
      context: .
      dockerfile: ./server/puppeteer/Dockerfile
    volumes:
      - "./server/puppeteer/:/app/"
      - "./output:/output"
    depends_on:
      - tor
      - node
  tor:
    build:
      context: .
      dockerfile: ./server/tor/Dockerfile
    expose:
      - "9150"